// vly-toolbar stub — minimal no-op for build when toolbar not needed in full-stack mode
// This file replaces the previous TypeScript version (vly-toolbar-readonly.tsx).
import React from "react";
export function VlyToolbar(){
  return null;
}
export default VlyToolbar;
